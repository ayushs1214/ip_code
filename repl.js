// Declare a variable and perform arithmetic operations
let a = 10;
let b = 5;
a + b // Press Enter to see the result, which should be 15

// Define a function and call it
function multiply(x, y) {
    return x * y;
}
multiply(3, 7) // Press Enter to execute the function and see the result

// Use the special variable "_" to access the result of the last expression
let result = _ // This will be the result of the last expression, which is the multiplication result

// Load a built-in Node.js module (fs for file system) and use it
const fs = require('fs');
fs.existsSync('/path/to/your/file.txt') // Check if a file exists by providing its path

// Create a simple object and access its properties
const person = {
    name: 'John',
    age: 30,
};
person.name // Access the 'name' property of the object

// Use tab completion to see available properties or methods
// person. // Type 'person.' and then press Tab to see available properties/methods

// Exit the REPL
// .exit
